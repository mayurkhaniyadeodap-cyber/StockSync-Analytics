/**
 * Chart colours.
 *
 * Every value is a design token, never a literal — that is what makes the
 * charts follow the light/dark switch without a second palette (design §16).
 */

/** Slice and series palette, in order. Ported verbatim from the prototype. */
export const CAT_COLORS = [
  'var(--slate)',
  'var(--moss)',
  'var(--amber)',
  'var(--clay)',
  'var(--rust)',
  'var(--ink-45)',
  'var(--line)',
];
